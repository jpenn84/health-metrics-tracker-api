spring:
  application:
    name: health-metrics-tracker-api
  jpa:
    defer-datasource-initialization: true
    open-in-view: false
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always
  mvc:
    format:
      date-time: iso